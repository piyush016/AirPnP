<h2>AirPnP</h2>
<h4>Things learnt: </h4>
<ol>
    <li>1. Components</li>
    <li>2. Props </li>
    <li>3. Array.map() : Where Array = Old Array 
    <span>const newArr = numbers.map(
        function myFunction(thing) {
            return (<p>{thing}</p>)
        }
    )</span></li>
</ol>